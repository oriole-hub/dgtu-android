# Скопируйте в `.env` при необходимости.
# Локальный веб (expo start --web): прокси /api/dstu в metro.config.js включается сам.
# Продакшен или свой прокси:
# EXPO_PUBLIC_API_BASE_URL=https://ваш-домен.ru/api/dstu/
